// src/common/enums/notificationType.enum.ts

export enum NotificationType {
    // Nhóm 1: type dành cho người mua đơn hàng
    ORDER = 'order',
    PROMOTION = 'promotion',

    // Nhóm 2: type dành cho nhân viên
    ORDER_TASK = 'order_task',
    INVENTORY = 'inventory',

    // Nhóm 3: hệ thống
    SYSTEM = 'system'

}